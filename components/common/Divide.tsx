import React from 'react'
import { DivideProps } from '@/interfaces'

const Divide: React.FC<DivideProps> = ({style}) => {
  return (
    <div className={`${style} bg-coloraccent`}>
    </div>
  )
}

export default Divide